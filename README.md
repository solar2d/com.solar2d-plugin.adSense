Sources for the plugin `plugin.adSense`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.adSense" = {
            publisherId = "com.solar2d",
        },
    },
}
```
